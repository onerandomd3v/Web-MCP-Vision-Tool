# Codespaces/local preview: point the client at the forwarded client origin.
# Vite proxies /operations, /auth, /api, /file, and /upload to the private Wasp server.
# REACT_APP_API_URL=https://<forwarded-port-3000-host>
# For a separately hosted production client, use the public Wasp server URL instead.
