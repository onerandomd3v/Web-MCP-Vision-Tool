[build]
builder = "RAILPACK"
buildCommand = "npm install && npx prisma generate && npm run build:server && npm run build:client"

[deploy]
startCommand = "npx prisma migrate deploy && npm run start:server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
