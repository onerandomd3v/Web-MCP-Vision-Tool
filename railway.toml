[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run db:generate && npm run build:server && npm run build:client"

[deploy]
startCommand = "npm run db:migrate && npm run start:server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
